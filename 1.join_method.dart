// What does join() method?
// Join method Convert each element to a String and concatenates the strings.

void main() {
  var numberList = [1, 2, 3, 4, 5, 6, 7, 8];
  var join = numberList.join(' ');
  print(join);
}
